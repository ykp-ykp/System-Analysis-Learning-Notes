## 记录一下后端现存的一些问题
1. 获取属性返回null：getParameterValue(instance, "frontprofiledataid ") 返回null，但是 instance.frontprofiledataid 却是有值的
2. 删除某个脚本的时候，如果存在依赖就删不掉，但是报错也不提示那些函数依赖了。
3. 发布某个脚本时，提示依赖未发布，希望提示具体的以来函数。
4. https://sit.kujiale.com/kb-editor/api/def/list 接口返回的对象属性确实。如`ModifiedCountertopLines`自定义对象缺失`waterRetainerType`属性

